const circle = {
  // radius and location are properties of the circle
  radius: 1,
  location: {
    x: 1,
    y: 1,
  },
  // draw is a method of the circle
  draw: function () {
    console.log('draw');
  },
};

circle.draw();
